// import { useState, useEffect } from 'react';
// import { getBomData } from '@services/bomService';
// import { BomItem } from '../types/bomTypes';

// const useBomData = () => {
//   const [data, setData] = useState<BomItem[] | null>(null);

//   useEffect(() => {
//     getBomData().then((response) => setData(response));
//   }, []);

//   return data;
// };

// export default useBomData;
