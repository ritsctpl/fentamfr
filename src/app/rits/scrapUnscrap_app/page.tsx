'use client';

import ScrapUnscrap from '@modules/scrapUnscrap/components/ScrapUnscrap';
import React from 'react';

const ScrapUnscrapApp: React.FC = () => {
  return <ScrapUnscrap filterFormData={undefined} selectedRowData={undefined} call2={undefined} phaseByDefault={undefined} selectedContainer={undefined} />
};

export default ScrapUnscrapApp;